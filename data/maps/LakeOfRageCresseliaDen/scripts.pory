mapscripts LakeOfRageCresseliaDen_MapScripts {}

script CresseliaEncounter {
    lock
    faceplayer
    legendaryencounter(SPECIES_CRESSELIA, 60, LOCALID_CRESSELIA, FLAG_HIDE_CRESSELIA)
    specialvar(VAR_RESULT, GetBattleOutcome)
    if (var(VAR_RESULT) == B_OUTCOME_CAUGHT) {
        if (var(VAR_POSTGAME_RIVAL_LEGENDARYQ) == 2) {
            setvar(VAR_POSTGAME_RIVAL_LEGENDARYQ, 3)
        }
    }
    release
    end
}